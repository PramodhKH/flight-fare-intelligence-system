# Portfolio assets

These images are intentionally tracked because they are referenced by the recruiter-facing root README.

- `dashboard_overview.png` — real Streamlit dashboard screenshot from the completed Phase 9 product.
- `model_accuracy_latency.png` — Phase 4 validation accuracy-versus-latency comparison.
- `high_fare_bias.png` — Phase 5 high-fare reliability diagnostic.
- `final_test_actual_vs_predicted.png` — Phase 10 held-out test diagnostic.
- `final_test_interval_coverage.png` — Phase 10 held-out conformal coverage by cabin class.

If the dashboard design changes after release, replace only `dashboard_overview.png` with a fresh screenshot using the same filename so README links remain stable.
