# Legacy project material

`Flight_Booking.pdf` is preserved as the original Intellipaat project reference.

The legacy workflow covered EDA, one-hot encoding, VIF-based feature selection, Linear Regression, Decision Tree, and Random Forest. It is retained for provenance only. New modeling and engineering decisions live in the package-based implementation under `src/`.
